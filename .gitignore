.cursor/
__pycache__/
output/
build/
.venv/
venv/
*.pyc
*.json
*.egg-info/

# A downloaded model repo cannot load without its config.json, which the
# blanket *.json rule above would otherwise drop.
!models/**/config.json

# Runtime YAML copies (e.g. saved training config under output/)
output/**/*.yaml

# Ignore local presets at config/*.yaml; track reference configs under config/examples/
# (Git cannot re-include files under an ignored directory — un-ignore each level explicitly.)
config/*
!config/examples/
!config/examples/**

data/
